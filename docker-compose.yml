version: '3'
services:
  backend:
    build: ./backend
    network_mode: "host"
    restart: always

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend
    restart: always
